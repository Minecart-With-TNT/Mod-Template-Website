# Separate Client

Whether to generate a separate client-only source set for client-side code.

When enabled, client-only classes live under a dedicated source set so they are not loaded on the dedicated server.
